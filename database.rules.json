{
  /* 
   * 🚨 WARNING: OPEN ACCESS FOR TESTING ONLY 
   * TODO: Change to auth-based rules before production:
   * ".read": "auth != null",
   * ".write": "auth != null"
   */
  "rules": {
    ".read": true,
    ".write": true
  }
}